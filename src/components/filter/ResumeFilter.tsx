import React from 'react';

const ResumeFilter = ({ onQueryChange, query }: { onQueryChange: (query: string) => void , query: string}) => {
    return (
        <div>

        </div>
    );
};

export default ResumeFilter;